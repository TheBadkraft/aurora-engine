// src/test/resources/valid_nesting.aml

test @[type=block] {
    config := {
        flags := [true, false],
        meta := { x := 1, y := 2 },
        items := [
            { id := 1 },
            { id := 2 }
        ]
    }
}